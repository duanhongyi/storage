
apiVersion: v1
kind: Secret
metadata:
  name: drycc-storage-creds
  labels:
    heritage: drycc
type: Opaque
data:
  {{- if and (neq .Value.global.certManagerEnabled "true") ( eq .Values.tls.enable "true" ) }}
  {{- $altNames := list ( printf "*.%s.%s" .Release.Namespace .Value.global.clusterDomain ) ( printf "*.%s.svc.%s" .Release.Namespace .Value.global.clusterDomain ) -}}
  {{- $ca := genCA "drycc-storage-ca" 3650 -}}
  {{- $cert := genSignedCert "drycc-storage-ca" nil $altNames 3650 $ca -}}
  ca.crt: {{ $ca.Cert | b64enc }}
  tls.crt: {{ $cert.Cert | b64enc }}
  tls.key: {{ $cert.Key | b64enc }}
  {{- end }}
  accesskey: {{ if .Values.accesskey | default "" | ne "" }}{{ .Values.accesskey | b64enc }}{{ else }}{{ randAlphaNum 32 | b64enc }}{{ end }}
  secretkey: {{ if .Values.secretkey | default "" | ne "" }}{{ .Values.secretkey | b64enc }}{{ else }}{{ randAlphaNum 32 | b64enc }}{{ end }}
